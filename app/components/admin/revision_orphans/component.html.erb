<div id="alert-border-4" data-controller="admin--temporary-message" class="flex items-center p-4 mb-4 text-yellow-800 border-t-4 border-yellow-300 bg-yellow-50 dark:text-yellow-300 dark:bg-gray-800 dark:border-yellow-800" role="alert"
     data-transition-enter-from="opacity-0 translate-x-6"
     data-transition-enter-to="opacity-100 translate-x-0"
     data-transition-leave-from="opacity-100 translate-x-0"
     data-transition-leave-to="opacity-0 translate-x-6" >
    <svg class="flex-shrink-0 w-4 h-4" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
      <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5ZM9.5 4a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3ZM12 15H8a1 1 0 0 1 0-2h1v-3H8a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1v4h1a1 1 0 0 1 0 2Z"/>
    </svg>
    <div class="ms-3 text-sm font-medium">
      <% if orphans.count == 1 %>
      There is an orphaned <%= record_type.downcase %> that can be <a class="underline" href="<%= orphaned_items_path("#{record_type.downcase}s") %>">retreived from the database</a>.
      <% else %>
      There are <%= orphans.count %> orphaned <% pluralize(orphans.count, record_type.downcase) %> that can be <a class="underline" href="<%= orphaned_items_path("#{record_type.downcase}s") %>"> retreived from the database</a>.
      <% end %>
      This data will be deleted with in 7 days if you do not edit it.
      <br>
    </div>
    <button type="button" data-action="admin--temporary-message#hide" class="ms-auto -mx-1.5 -my-1.5 bg-yellow-50 text-yellow-500 rounded-lg focus:ring-2 focus:ring-yellow-400 p-1.5 hover:bg-yellow-200 inline-flex items-center justify-center h-8 w-8 dark:bg-gray-800 dark:text-yellow-300 dark:hover:bg-gray-700" data-dismiss-target="#alert-border-4" aria-label="Close">
      <span class="sr-only">Dismiss</span>
      <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
      </svg>
    </button>
</div>